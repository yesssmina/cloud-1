---
# Domain — used by Nginx reverse proxy and TLS certificate
domain_name: sanaggar-wnaiji.duckdns.org

# Email for Let's Encrypt certificate notifications
certbot_email: sanaggar@student.42nice.fr

# Database — shared between MySQL, WordPress and phpMyAdmin
db_name: wordpress
db_user: wp_admin

# Secrets — pulled from vault.yml (encrypted), never hardcoded
db_password: "{{ vault_db_password }}"
db_root_password: "{{ vault_db_root_password }}"
